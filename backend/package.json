{
  "name": "multi-send-atomic-protocol",
  "version": "1.0.0",
  "description": "MultiSend Atomic Transfer Protocol smart contract in Solidity",
  "main": "index.js",
  "scripts": {
    "build": "forge build",
    "test": "forge test -vvv"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.2"
  },
  "devDependencies": {
    "solc": "^0.8.36"
  }
}
